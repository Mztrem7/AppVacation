let btn = document.getElementById('btn');
let container = document.getElementsByClassName('container');

const submit = (city, start, end) => {
    // Post axios
}

const showData = () => {
    container.style.display = 'none'
}

btn.addEventListener('click', submitData)